@{
    // This can be overridden by adding a _ViewStart page to each controller.
    Layout = "~/Views/Shared/_Layout.cshtml";
    // Typically you wouldn't have two Layout pages. If you're doing razor pages then stick to razor pages and vice versa.
}
